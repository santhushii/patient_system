import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Ensure you have this file or remove this line
import App from './App'; // Ensure you create an App.js file or replace it with a default component

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

